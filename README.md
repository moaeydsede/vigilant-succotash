# Customer Evaluation – CRM Score (VIP) v2.3.0

✅ Frontend Only (بدون سيرفر)  
✅ عربي 100٪ + RTL  
✅ Mobile First (VIP UI)  
✅ LocalStorage (سريع جدًا)  
✅ Excel Import/Export (SheetJS)  
✅ PDF (A4) (jsPDF + AutoTable)  
✅ PWA (تثبيت كتطبيق + Offline Cache)

---

## 1) تشغيل محليًا
افتح الملف:
- `index.html`

> ملاحظة: الـ Service Worker يعمل بشكل أفضل على GitHub Pages أو أي استضافة HTTPS.

---

## 2) نشر على GitHub Pages (الأفضل)
1. أنشئ Repo جديد في GitHub.
2. ارفع محتويات المجلد `Customer_Evaluation_CRM_Score/` داخل الريبو.
3. اذهب إلى:
   - Settings → Pages
4. اختر:
   - Branch: `main`
   - Folder: `/ (root)`
5. سيفتح الرابط.

---

## 3) الحساب الرئيسي
- Username: `admin`
- Password: `admin123`
- Role: Admin

✅ فقط Admin يستطيع دخول صفحة "المستخدمون" وإضافة/حذف مستخدمين.

---

## 4) مفاتيح LocalStorage
- `crm_users_v2`
- `crm_session_v2`
- `crm_company_v2`
- `crm_firebase_v2`
- `crm_local_db_v2`
- `crm_ops_log_v2`

---

## 5) ملاحظات
- هذه نسخة "Frontend Only" — أي مزامنة Firebase غير مفعلة افتراضيًا (فقط حفظ إعدادات).
- لتصفير كل شيء: من القائمة الجانبية (Admin فقط) → "مسح البيانات المحلية".

