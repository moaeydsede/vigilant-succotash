# Customer Evaluation – CRM Score (Frontend Only)

نظام ويب (Frontend Only) لتقييم العملاء تلقائيًا اعتمادًا على:
- المبيعات
- المرتجعات
- السداد
- الخصومات

يعرض النتيجة كنسبة مئوية + نجوم + تصنيف بصري (VIP / جيد / متوسط / تنبيه / خطر).
مصمم **Mobile First** ويعمل بسرعة عالية على الموبايل.

---

## ✅ المميزات
- عربي 100% (RTL)
- Frontend Only (بدون Backend)
- يعمل على GitHub Pages
- بحث + فلترة + ترتيب
- Excel (استيراد/تصدير) عبر SheetJS
- PDF A4 احترافي عبر jsPDF + autoTable
- إدارة مستخدمين محليًا (LocalStorage)
- **Firebase Firestore اختياري** (Spark مجاني)

---

## 📁 هيكل المشروع
```
Customer_Evaluation_CRM_Score/
├── index.html
├── app.css
├── app.js
└── README.md
```

---

## 🔐 تسجيل الدخول
الحساب الافتراضي:
- Username: `admin`
- Password: `admin123`
- Role: `Admin`

الأدوار:
- Admin: كل الصلاحيات
- Accountant: إدارة البيانات (إضافة/تعديل/حذف/Excel/Sync)
- Viewer: عرض فقط

> ملاحظة أمان: المستخدمون محليون داخل الجهاز (LocalStorage). للاستخدام متعدد المستخدمين بشكل احترافي استخدم Firebase Authentication + Security Rules.

---

## 🔥 تفعيل Firestore (اختياري)
1) أنشئ مشروع Firebase (Spark).
2) فعّل Firestore Database.
3) من داخل النظام: **بيانات الشركة → Firebase Config**
4) ألصق إعدادات Firebase (apiKey, projectId...).
5) اضغط **اختبار الاتصال** ثم استخدم زر **مزامنة**.

> بدون إعداد Firebase سيعمل النظام محليًا فقط.

---

## 📤 Excel
- تصدير: يولّد ملف `CRM_Score_Export_YYYY-MM-DD.xlsx`
- استيراد: يدعم الشيتات التالية:
  - `customers`
  - `sales`
  - `returns`
  - `payments`
  - `discounts`

استخدم زر **تحميل قالب** لتجنب الأخطاء.

---

## 🖨️ PDF
- أفضل العملاء (Top 20)
- كشف حساب عميل (مع مدة اختيارية)

---

## 🚀 تشغيل على GitHub Pages
1) ارفع الملفات الأربعة إلى مستودع GitHub.
2) من Settings → Pages:
   - Source: Deploy from a branch
   - Branch: main (أو master) / root
3) افتح رابط Pages.

---

## 🧪 Demo
في شاشة الدخول اضغط **تهيئة سريعة (Demo)** لإنشاء بيانات تجريبية محلية.

---

## 🧮 Score Engine
الأوزان:
- Sales: +0.4
- Returns: −0.3
- Payments: +0.2
- Discounts: −0.1

يتم تحويل الناتج إلى نطاق 0..100 بشكل عملي (مع ضبط تلقائي حسب المبيعات).
